# Myna Fix Report — Cycle 003

**Date:** 2026-04-09
**Mode:** Auto (pipeline)
**Review:** `docs/reviews/review-003.md`

## Actions

### [I01] Reply-needed TODO routing inconsistency

**Action:** Implemented Option 1 — write reply-needed TODOs directly to project files as `[Inferred]` Tasks plugin TODOs
**Changes:**
- `process.md` step 4d: Replaced "Route reply-needed TODOs to `review-work.md`" with explicit instructions for writing reply-needed TODOs directly to the relevant project file's `## Open Tasks` section as Obsidian Tasks plugin TODOs. Added the canonical format with required inline properties (`[type:: reply-needed]`, `[review-status:: pending]`, `[Inferred]`) and source reference. Documented why `[Inferred]` is the right marker (the "needs reply" judgment is the agent's interpretation). Documented why the format must NOT include `[person:: ...]` (so brief.md's "Waiting on you" categorization works — see verify-003.md for the full reasoning on why this matters). Added fallback: if project can't be resolved, write to today's daily note. Reworked auto-resolve to use the Obsidian MCP `tasks` query to find matching reply-needed TODOs in project files, narrowing by sender + thread context.

**Files modified:** `agents/skills/process.md`

**Why this option:** Brief.md's Unreplied Tracker query (`tasks` MCP, `type:: reply-needed`) only finds TODOs with inline properties — review queue entries don't have these. Writing directly to project files with `[Inferred]` and `[review-status:: pending]` makes the TODOs queryable, gives the user a verification flag, and lets the auto-resolve mechanism work correctly. This matches the [Inferred] decision criterion ("core item real, some fields guessed") and aligns process.md with brief.md's existing query model.

---

### [M01] draft.md frontmatter type list missing performance-narrative and pre-read

**Action:** Implemented Option 1 — added the missing values to the frontmatter type list
**Changes:**
- `draft.md` line 153 (Output section, Draft file format): Added `pre-read` and `performance-narrative` to the frontmatter `type` enumeration. Full updated list: email-reply, follow-up, status-update, escalation, recognition, meeting-invite, say-no, conversation-prep, **pre-read**, monthly-update, self-review, promo-packet, brag-doc, **performance-narrative**.

**Files modified:** `agents/skills/draft.md`

**Why this option:** The list pretends to be exhaustive, so it should be exhaustive. Adding two values is minimal overhead and preserves the at-a-glance reference value of having all draft types in one place.

---

### [M02] sync.md prep format unspecified

**Action:** Implemented Option 2 — added a Rules note pointing at the canonical format
**Changes:**
- `sync.md` Rules section (after "Lightweight meeting preps"): Added a new rule "**Prep item format matches prep-meeting.**" with the four bold-header patterns (`**Carry-forward:**`, `**Recent work:**`, `**Pending feedback:**`, `**Action item:**`) and explained why consistency matters (prep-meeting's update mode dedup; brief.md's topic-source-balance heuristic).

**Files modified:** `agents/skills/sync.md`

**Why this option:** The rule explicitly references the consistency requirement and the two consumers that depend on it, without bloating the procedure section. The format itself is already shown in prep-meeting.md's worked example — sync.md just needs to commit to using it.

---

### [M03] Performance narrative default time period mismatch

**Action:** Implemented Option 1 — changed brief.md default to 3 months
**Changes:**
- `brief.md` line 118 (Performance Narrative step 2): Changed "Default: last 6 months." to "Default: last 3 months." Now matches the feature spec in `docs/features/people-management.md` and aligns with the typical quarterly review cadence.

**Files modified:** `agents/skills/brief.md`

**Why this option:** The feature spec is the source of truth for sub-feature defaults. 3 months is also the more common review cycle, so the default is more useful. Trivial fix with no downstream impact since the period is overridable per request.

---

### [M04] wrap-up.md self-reflection feedback gap toggle gating

**Action:** Implemented Option 1 — added explicit toggle check for the feedback gap reflection
**Changes:**
- `wrap-up.md` Weekly Summary step 4 (Self-Reflection bullet): Removed "feedback gaps (days since last feedback to each direct report, if manager)" from the unconditional list. Added a sentence at the end of the bullet: "If `features.feedback_gap_detection` is enabled and the user is a manager, also include feedback gaps (days since last feedback to each direct report). If `feedback_gap_detection` is disabled, omit the feedback gap reflection."

**Files modified:** `agents/skills/wrap-up.md`

**Why this option:** Consistent with the cycle 001 fix that wired the toggle to its function in brief.md and prep-meeting.md. A user who disables feedback_gap_detection wants ALL feedback gap nudges silenced, not a subset.

---

### [M05] process-meeting.md `[Processed]` marker scope ambiguous for 1-1 files with multiple sessions

**Action:** Implemented Option 1 — clarified the scan and the marker placement to be per-session
**Changes:**
- `process-meeting.md` step 1 (Locate, batch path): Replaced ambiguous "files with a `## {today's date} Session` header that has a Notes section with content but no `[Processed]` marker" with explicit per-session scoping: "any session matching today's date — that is, a `## {today's date} Session` header where the Notes section beneath that header is non-empty and no `*[Processed {today's date}]*` marker appears within that session (between the session header and the next `## ` heading or end of file). The marker is dated and per-session — a 1:1 file with multiple sessions may have multiple `*[Processed {date}]*` markers, one per processed session."
- `process-meeting.md` step 7 (Mark as Processed): Added explicit per-session placement guidance: "append a marker below this session's Notes section (and before the next `## ` session heading, if any)". Reinforced that the marker is per-session and that multiple markers in a 1:1 file are expected.

**Files modified:** `agents/skills/process-meeting.md`

**Why this option:** Minimal change — keeps the existing marker format and scan approach, just makes the scope explicit. The marker is already dated, so an LLM interpreting the per-session scoping correctly can find it. The fix is wording clarification only.

---

## Self-Verification

After all fixes:

1. **Each modified file re-read in full** — process.md, draft.md, sync.md, brief.md, wrap-up.md, process-meeting.md. All edits sit naturally in their surrounding context. No contradictions introduced. The reply-needed TODO format in process.md aligns with the canonical task format in conventions.md. The sync.md prep format rule references the same patterns prep-meeting.md uses.

2. **Cross-skill consistency check on shared destinations:**
   - **Reply-needed TODOs:** process.md now writes them as Tasks plugin TODOs in project files with `type:: reply-needed`. brief.md's `tasks` MCP query for `type:: reply-needed` will now find them. Auto-resolve uses the same `tasks` query mechanism. Consistent.
   - **Prep item format:** sync.md and prep-meeting.md now both use `**Carry-forward:**`, `**Recent work:**`, `**Pending feedback:**`, `**Action item:**` patterns. brief.md's heuristic recognizes these. Consistent.
   - **Draft type list:** draft.md frontmatter list now includes all 14 types (added pre-read, performance-narrative). brief.md and self-track.md generate their drafts using the values in this list. Consistent.
   - **Performance narrative default:** brief.md and people-management.md feature spec now both say 3 months. Consistent.
   - **Feedback gap toggle:** brief.md (cycle 001), prep-meeting.md (cycle 001), and wrap-up.md (this cycle) all gate feedback gap behavior on `features.feedback_gap_detection`. Consistent.
   - **Processed marker:** process-meeting.md is the only file that touches this. Internal consistency only.

3. **Safety check:** No new write paths outside the vault. No send/post/deliver paths. No accidental bulk-write paths without confirmation. Calendar three-layer protection unchanged. Reply-needed TODO writes go to project files (which is already a known safe destination) — no new safety surface.

4. **Lint verification:** Re-ran `bash scripts/lint-agents.sh` after all fixes. Result: **0 errors, 8 warnings** (same 8 false positives as before, no new ones). Status: PASS. The changes did not introduce any self-containment violations, missing sections, or new safety keyword instances.

## Summary

- **Issues fixed:** 6 (1 Important, 5 Minor)
- **Issues pushed back:** 0
- **Issues skipped:** 0
- **Files modified:** `agents/skills/process.md`, `agents/skills/draft.md`, `agents/skills/sync.md`, `agents/skills/brief.md`, `agents/skills/wrap-up.md`, `agents/skills/process-meeting.md`
