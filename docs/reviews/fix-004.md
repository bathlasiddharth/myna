# Myna Fix Report — Cycle 004

**Date:** 2026-04-10
**Mode:** Auto (from improvement pipeline)
**Review:** `docs/reviews/review-004.md`

## Actions

### [I01] Auto-tagging feature not operationalized
**Action:** Implemented Option 3 (narrow to project-based and person-based tags, defer keyword/source-based to post-v1)

**Changes:**
1. Updated `agents/steering/conventions.md` Tags section:
   - Removed the misleading "Auto-applied by the tagging system" claim
   - Added "Tag application at file creation" sub-section with explicit rules for project files (look up `tags.yaml` with `type: project-based` + matching project) and person files (use `relationship_tier` from people.yaml + optional person-based tag from tags.yaml)
   - Added explicit deferral note: "Other tag rule types are deferred to post-v1. `tags.yaml` may contain `keyword-based`, `source-based`, and other rule types — these are not applied automatically in v1."
2. Updated `agents/main.md` File Creation from Template direct operation:
   - Added an explicit slug convention line ("File slugs use lowercase with hyphens for spaces (e.g., 'Auth Migration' → `auth-migration.md`, 'Sarah Chen' → `sarah-chen.md`).") — also addresses M05 self-containment gap around slug format
   - Project file line: "Apply project-based tags from `tags.yaml` when substituting the template's tag placeholder (see conventions.md Tags section)."
   - Person file line: "Apply person-based tags and the relationship tier tag from `tags.yaml` and `people.yaml` (see conventions.md Tags section)."

**Files modified:** `agents/steering/conventions.md`, `agents/main.md`

**Why this closes the gap:** The `{project-tag}` placeholder in the foundation's project template now has a defined source (tags.yaml project-based entries) and invocation point (main.md File Creation). The `#{relationship-tier}` placeholder in the person template has a defined source (people.yaml `relationship_tier` field). Keyword-based and source-based rules are explicitly deferred so conventions.md no longer makes promises it can't keep.

---

### [I02] Slack unreplied tracking uses email-specific criteria
**Action:** Implemented Option 1 (broaden step 4d's criteria to handle both email and Slack)

**Changes:**
1. Updated `agents/skills/process.md` step 4d (Unreplied tracking):
   - Replaced "(1) you are in the To or CC field (matched against `user.email`)" with a source-aware condition: "(1) the message is directed at you — for email: you are in the To or CC field (matched against `user.email`); for Slack: the message is a DM to you, @mentions you (matched against `user.name` or Slack handle from `people.yaml`), or comes from a direct report in a channel where you're the primary recipient"
   - Expanded the skip list to include "FYI broadcasts, and messages where you are one of many recipients without being specifically addressed"
2. Updated the reply-needed TODO format block to show both email and Slack source formats:
   ```
   - [ ] Reply to {sender first name} about {topic} [project:: ...] [type:: reply-needed] [review-status:: pending] [Inferred] (email, {sender first name}, {date}) — needs reply
   - [ ] Reply to {sender first name} about {topic} [project:: ...] [type:: reply-needed] [review-status:: pending] [Inferred] (slack, #{channel or sender first name}, {date}) — needs reply
   ```
   The "Format" line now reads "Format (use the source type that matches — `email` or `slack`)".
3. Updated the Auto-resolve paragraph to handle both sources:
   - Old: "if a later email in the same thread is FROM `user.email`"
   - New: "if a later message in the same thread (email thread or Slack thread) is FROM the user (matched against `user.email` for email or `user.name`/Slack handle for Slack)"
   - Also changed "the same mechanism used for any task completion" reference from email-only to source-agnostic.

**Files modified:** `agents/skills/process.md`

**Why this closes the gap:** Slack messages now have well-defined criteria for triggering reply-needed TODO creation (DM / @mention / direct-report channel). Process.md step 8 ("For each message, apply the same extraction logic as email ... including unreplied tracking (step 4d)") is now accurate — step 4d works for both. The feature spec's promise ("during email processing, messaging processing, or email triage, the agent flags messages that need a reply") is now kept for messaging processing too.

---

### [I03] Review queue entry format not specified in runtime context
**Action:** Implemented Option 1 (add Review Queue Entries canonical format to conventions.md)

**Changes:**
1. Added a new sub-section "Review Queue Entries (review-work.md, review-people.md, review-self.md)" to `agents/steering/conventions.md` under Canonical Entry Formats, following the existing pattern for Timeline, Observations, Recognition, and Contributions:
   ```
   - [ ] **{proposed action}**
     Source: {source type, identity, date — e.g., "email from Sarah, 2026-04-05" or "meeting: 1:1 with Sarah, 2026-04-06"}
     Interpretation: {what the agent thinks this is — task, observation, decision, etc.}
     Ambiguity: {why it's in the queue — what's unclear or why a reasonable person might disagree}
     Proposed destination: {file path and section where it would be written if approved}
     ---
   ```
2. Added clarifying notes:
   - "The `---` separator between entries is literal — it helps the review skill parse items and the user visually separate them in Obsidian."
   - "Review-triage.md uses a different, simpler format (see triage.md's Output section) — it only recommends folder moves, not vault updates, and has its own entry format." (Addresses the known difference between review-triage and other review queues.)
   - "User actions: check the box (approve), edit the text (modify), delete the entry (discard), leave unchecked (skip for later). The review skill uses this format when presenting items in chat mode and when processing checked items in file mode."

**Files modified:** `agents/steering/conventions.md`

**Why this closes the gap:** Skills that write to review queues (capture, process, process-meeting, wrap-up) already defer to "conventions.md" for entry formats per the Rules sections in each skill. The format now lives in the place they're already pointed at. The review.md skill reads queue files expecting this format, and its worked example still matches. No skill needs procedural changes — the canonical format is now discoverable in the runtime context.

---

### [I04] main.md Task Completion and Task Move direct ops only search project files
**Action:** Implemented Option 2 (use the Obsidian MCP `tasks` query for both operations)

**Changes:**
1. Updated `agents/main.md` Task Completion direct operation:
   - Old: "Find the matching TODO in project files using fuzzy matching. Mark it as complete."
   - New: "Query the Obsidian MCP `tasks` tool to find open TODOs matching the description via fuzzy matching. The query spans all markdown files in the vault — project files, daily notes, meeting files, wherever the task lives. If multiple matches, ask the user to pick. If one match, mark it complete (`- [x]`). Confirm: 'Marked complete: {task description} in {file}.'"
2. Updated `agents/main.md` Task Move direct operation step 1:
   - Old: "Find the task using fuzzy matching in source project files."
   - New: "Query the Obsidian MCP `tasks` tool to find the task via fuzzy matching on the description. The query spans all markdown files in the vault — project files, daily notes, meeting files. If multiple matches, ask the user to pick. The source can be any file, not just a project file (e.g., a personal task in the daily note can be moved to a project)."

**Files modified:** `agents/main.md`

**Why this closes the gap:** Both operations now use the vault-wide `tasks` MCP query instead of a scoped file-location search. Tasks in daily notes (created by capture.md when no project context) are now findable. Tasks in meeting files, review queue processed files, and anywhere else tasks legitimately live are also findable. The ambiguity handling (multiple matches → ask) is preserved.

---

### [M01] prep-meeting.md `debrief_type` in Inputs but unused in procedure
**Action:** Implemented Option 1 (remove dead reference)

**Changes:**
Updated `agents/skills/prep-meeting.md` line 21 Inputs — removed `debrief_type` from the meetings.yaml input list:
- Old: "**meetings.yaml**: type overrides, project associations, aliases, `debrief_type`"
- New: "**meetings.yaml**: type overrides, project associations, aliases"

**Files modified:** `agents/skills/prep-meeting.md`

---

### [M02] wrap-up.md weekly note creation references sync's process (self-containment)
**Action:** Implemented Option 3 (use `create-from-template` via MCP with minimal fallback)

**Changes:**
Updated `agents/skills/wrap-up.md` Weekly Summary step 1:
- Old: "Read the current weekly note `Journal/WeeklyNote-{monday-date}.md`. If it doesn't exist, create it (same process as sync's weekly note creation)."
- New: "Read the current weekly note `Journal/WeeklyNote-{monday-date}.md`. If it doesn't exist, create a minimal one: use `create-from-template` with `_system/templates/weekly-note.md` if the template exists; otherwise `write` a new file with frontmatter `week_start: {monday-date}`, the `#weekly` tag, and empty sections `## Week Capacity`, `## Weekly Goals`, `## Carry-Forwards`. Wrap-up does not populate the Week Capacity table or Carry-Forwards — those are sync's responsibility. Wrap-up's job is to append the Weekly Summary section below whatever is already present."

**Files modified:** `agents/skills/wrap-up.md`

**Why this closes the gap:** Wrap-up no longer depends on sync.md being loaded in context. The minimal creation is self-contained (template path + fallback write with explicit frontmatter and sections), and the responsibility boundary with sync is explicit ("wrap-up does not populate the Week Capacity table or Carry-Forwards").

---

### [M03] capture.md task type list missing `reply-needed`
**Action:** Implemented Option 1 (add reply-needed to the type list)

**Changes:**
Updated `agents/skills/capture.md` step 4 (Task Creation) attribute extraction:
- Old: "type (task, delegation, dependency)"
- New: "type (task, delegation, dependency, reply-needed)"

**Files modified:** `agents/skills/capture.md`

**Why this closes the gap:** brief.md's Unreplied Tracker suggests users create reply-needed tasks via capture. Capture's type list now includes it. `retry` stays out of the user-facing list since it's agent-generated only (error recovery).

---

### [M04] Confirmation policy threshold inconsistency
**Action:** Implemented Option 2 (align all three files to "5 or more" — the stricter, clearer phrasing)

**Changes:**
1. `agents/steering/safety.md`:
   - Old: "Bulk writes (a single operation writing to **more than 5** vault files)"
   - New: "Bulk writes (a single operation writing to **5 or more** vault files)"
2. `agents/skills/process.md` step 4f:
   - Old: "If writing to **5+ files** in a batch or processing 10+ emails"
   - New: "If writing to **5 or more files** in a batch or processing **10 or more** emails"
3. `agents/skills/draft-replies.md` bulk write check:
   - Old: "If **5+ emails** are in the folder"
   - New: "If **5 or more emails** are in the folder"

**Files modified:** `agents/steering/safety.md`, `agents/skills/process.md`, `agents/skills/draft-replies.md`

**Why this closes the gap:** All three files now use the same threshold (5 files = trigger confirmation) and the same unambiguous phrasing ("5 or more"). The stricter interpretation (5 rather than 6) is safer for bulk operations.

---

### [M05] wrap-up.md example uses invalid source value `wrap-up`
**Action:** Implemented Option 1 (add `wrap-up` to conventions.md's approved source list)

**Changes:**
Updated `agents/steering/conventions.md` Date and Source Format → Source values list:
- Added: "`wrap-up` — from end-of-day scanning (agent-detected contributions from completed work aggregated by the wrap-up skill)"

The wrap-up.md example entries (`- [2026-04-06 | wrap-up] ...`) are now consistent with conventions.md. No changes needed to wrap-up.md itself — its example was already correct, it was conventions.md that was missing the source value.

**Files modified:** `agents/steering/conventions.md`

**Why this closes the gap:** Wrap-up's end-of-day scan is a distinct source category (agent-triggered aggregation, not external data extraction or user typing). Adding it to the canonical list acknowledges this while keeping the source-value set complete. wrap-up.md's Rules entry "Entry formats follow conventions.md" is now satisfied by the example.

---

### [M06] calendar.md no fallback defaults for `calendar_event_types`
**Action:** Implemented Option 1 (inline defaults in calendar.md)

**Changes:**
Added a "Config defaults" paragraph at the top of calendar.md's Procedure section (before the Time Block Planning sub-section):
> "**Config defaults.** If `calendar_event_prefix` is missing from workspace.yaml, use `[Myna]`. If `calendar_event_types` or any of its sub-fields (`focus`, `task`, `reminder`) are missing, use defaults: `focus: Focus`, `task: Task`, `reminder: Reminder`. These defaults keep the three-layer protection intact when config is sparse or partially customized."

**Files modified:** `agents/skills/calendar.md`

**Why this closes the gap:** The existing default for `calendar_event_prefix` is now matched by explicit defaults for `calendar_event_types`. A user who customizes workspace.yaml and accidentally removes these fields still gets functional calendar events with the standard prefix format. Layer 2 (pre-tool check) still works because the title format will have the prefix.

---

### [M07] brief.md 1:1 Pattern Analysis missing `features.people_management` toggle check
**Action:** Implemented Option 1 (add toggle check at step 1)

**Changes:**
Updated `agents/skills/brief.md` 1:1 Pattern Analysis step 1:
- Old: "Resolve the person name against `people.yaml`. If ambiguous, ask."
- New: "Check `features.people_management`. If disabled, tell the user and stop. Then resolve the person name against `people.yaml`. If ambiguous, ask."

**Files modified:** `agents/skills/brief.md`

**Why this closes the gap:** Consistent with Performance Narrative's gating pattern (which also checks `features.people_management` at step 1). Users who disable people_management now have all four people-management brief features correctly gated: Person Briefing (partial gate of feedback sections), Team Health Overview (full stop), Performance Narrative (full stop), and now 1:1 Pattern Analysis (full stop).

---

## Self-Verification

After all fixes, re-read each modified file in full and cross-checked:

**Internal consistency (per file):**
- `conventions.md`: The new Review Queue Entries section slots naturally into the existing Canonical Entry Formats sequence (Timeline → Observations → Recognition → Contributions → Review Queue). The Tag application rules fit naturally in the Tags sub-section. The `wrap-up` source value is added in the list without disrupting the other entries.
- `main.md`: Task Completion and Task Move now share the same approach (tasks MCP query, fuzzy match, ambiguous = ask). File Creation's new slug convention line is at the top of the operation, before the project/person file details.
- `process.md`: Unreplied tracking step 4d reads cleanly — the source-aware condition and the updated format block are consistent. Auto-resolve now matches the step 4d criteria. The "5 or more" wording in step 4f is consistent with safety.md.
- `wrap-up.md`: Weekly Summary step 1 is now self-contained. The example contributions (using `wrap-up` source) are now consistent with conventions.md's approved list.
- `brief.md`: 1:1 Pattern Analysis step 1 now matches Performance Narrative's gating pattern.
- `calendar.md`: Config defaults at top of Procedure are visible before any step that uses them.
- `capture.md`: Task type list matches conventions.md's `[type:: ...]` approved values.
- `prep-meeting.md`: Inputs list no longer has a dead reference.
- `draft-replies.md`: Bulk write wording matches safety.md.
- `safety.md`: Confirmation policy wording is clearer and matches skills.

**Cross-file consistency:**
- **Tag application:** main.md File Creation references conventions.md Tags section; conventions.md Tags section defines the rules. Closed loop.
- **Review queue entry format:** conventions.md now has the canonical format; capture/process/process-meeting/wrap-up already defer to conventions.md in their Rules sections; review.md reads entries with matching expectations.
- **Source value list:** conventions.md's approved list now includes all 6 values (email, slack, meeting, capture, user, wrap-up). wrap-up.md's example is consistent.
- **Slack unreplied:** process.md step 4d handles both email and Slack; brief.md's Unreplied Tracker queries `type:: reply-needed` via MCP — no distinction needed, the tasks are the same type with different source values in the source reference.
- **Bulk write threshold:** safety.md, process.md, and draft-replies.md all say "5 or more" now. Uniform wording.
- **Task completion scope:** main.md uses `tasks` MCP query (vault-wide). Capture.md's rule that tasks without a project go to daily note is now compatible because the task completion op can find them.
- **Toggle gating for people-management:** brief.md has all 4 people features gated (Person Briefing partial, Team Health full stop, Performance Narrative full stop, 1:1 Pattern Analysis full stop — new).

**Safety check:** No fix introduced a send/post/deliver path. No fix altered the draft-never-send rules. No fix introduced vault writes outside `myna/`. Calendar three-layer protection is unchanged. The new `wrap-up` source value is just a label — it doesn't unlock any new write behavior.

**Lint verification:** Re-ran `bash scripts/lint-agents.sh` after all fixes. **0 errors, 8 warnings** (same false positives as cycles 001-003 — `Review and send` user-action TODOs, example content). Status: PASS.

## Summary

| Metric | Count |
|--------|-------|
| Issues addressed | 11 |
| Issues pushed back | 0 |
| Issues skipped | 0 |
| Files modified | 10 |

**Files modified:**
- `agents/main.md`
- `agents/steering/conventions.md`
- `agents/steering/safety.md`
- `agents/skills/process.md`
- `agents/skills/prep-meeting.md`
- `agents/skills/capture.md`
- `agents/skills/wrap-up.md`
- `agents/skills/calendar.md`
- `agents/skills/brief.md`
- `agents/skills/draft-replies.md`
